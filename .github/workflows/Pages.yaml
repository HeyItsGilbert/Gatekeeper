name: Update Github Pages
on:
  push:
    branches:
      - main
  workflow_dispatch:
permissions:
  contents: write
jobs:
  ci:
    uses: HeyItsGilbert/.github/.github/workflows/Pages.yaml@main
